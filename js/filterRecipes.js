// This is the function to filter the list of recipes.
// It returns a new list to render

// Waiting pull request of one of each version of algorithm
// Check out the branches search-algo-a and search-algo-b
const filterRecipes = ({ recipes, tags, search }) => {
  let result = [];

  void recipes;
  void tags;
  void search;
  return result;
};

export { filterRecipes };
